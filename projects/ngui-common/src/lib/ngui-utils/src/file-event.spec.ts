import { async } from '@angular/core/testing';
import 'jest';

describe('fireEvnet', () => {
  // let fixture;
  // let component;

  beforeEach(async(() => {
    //
  }));

  it('should pass', async(() => {
    // expect(component).toBeTruthy();
  }));
});
