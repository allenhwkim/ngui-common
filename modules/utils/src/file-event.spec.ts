import { async, TestBed } from '@angular/core/testing';

describe('fireEvnet', () => {
  // let fixture;
  // let component;

  beforeEach(async(() => {
    //
  }));

  it('should pass', async(() => {
    // expect(component).toBeTruthy();
  }));
});
