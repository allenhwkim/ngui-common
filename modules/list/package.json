{
  "name": "@ngui/common/list",
  "scripts": {
    "test": "cd ../.. && jest modules/list/**/*.spec.ts",
    "doc": "cd ../../ && compodoc modules -p tsconfig.json && open -a 'Google Chrome' documentation/modules.html"
  }
}
