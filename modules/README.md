## For developers, to install/test all modules once

- `$ npm install`
- `$ npm-install-peers`

## Commands

- `$ npm test`, to run all unit tests of all modules
- `$ npm run build`, to build all modules into a single package